# Search Notes

## Safe Queries Used

- infrared small target detection multi-scale fusion architecture CVPR AAAI
- infrared small target network architecture UIU-Net DNANet SCTransNet
- coarse-to-fine residual logit refinement semantic segmentation pyramid
- Laplacian pyramid residual prediction semantic segmentation network
- orthogonal frequency band multi-scale fusion semantic segmentation
- mutually orthogonal scale subspace fusion segmentation logits

## Sources Checked

- CVF Open Access
- AAAI/OJS proceedings
- arXiv
- OpenReview
- primary paper/project records found through web discovery

## Excluded Sources

- Policy-excluded domains and publications were omitted from the candidate table.
- Search snippets without an inspectable stable paper record were not used for claims.
- Generic blog, Reddit, Wikipedia, and low-signal venue results were discarded.

## Unknowns

- A fully exhaustive patent and non-English literature search has not been performed.
- “Orthogonal Scale Ownership” as an exact phrase is not evidence of novelty; mechanism-level overlap must continue to be monitored.
- Current OSO performance is unknown until the fixed-endpoint paired gate completes.

## 2026-07-13 Queries and Decisions

- differentiable persistent homology segmentation false-positive connected components
- component persistence across feature-pyramid scales
- learnable top-k / rank / ordinal pooling
- channel-consensus spatial argmax pooling
- counterfactual leave-one-peak max pooling

New decision: ordinary rank/top-k pooling is occupied prior art.  The present
SPT run is therefore a mechanism screen, not an AAAI novelty claim.  The claim
can survive only if the strongest-site deletion counterfactual, channel
consensus, and one scale-transport law are jointly necessary under component
FROC evidence.

## Handoff Notes

- For idea optimization: position OSO as identifiable multi-source fusion, not a Laplacian refinement network.
- For experiment design: compare canonical fusion, unstructured sum, ordinary Laplacian residual head, OSO, and OSO with/without responsibility training under shared prefixes.
- For review: the most dangerous objection is “fixed Laplacian bands applied to MSHNet”; require source-aligned orthogonality diagnostics and gains beyond generic filtering controls.
